;==========================================================
;
;
;   ██████╗  ██████╗ ██╗  ██╗   ██╗██████╗  █████╗ ██████╗
;   ██╔══██╗██╔═══██╗██║  ╚██╗ ██╔╝██╔══██╗██╔══██╗██╔══██╗
;   ██████╔╝██║   ██║██║   ╚████╔╝ ██████╔╝███████║██████╔╝
;   ██╔═══╝ ██║   ██║██║    ╚██╔╝  ██╔══██╗██╔══██║██╔══██╗
;   ██║     ╚██████╔╝███████╗██║   ██████╔╝██║  ██║██║  ██║
;   ╚═╝      ╚═════╝ ╚══════╝╚═╝   ╚═════╝ ╚═╝  ╚═╝╚═╝  ╚═╝
;
;
;   To learn more about how to configure Polybar
;   go to https://github.com/polybar/polybar
;
;   The README contains a lot of information
;
;==========================================================

[colors]
background = #914f2d
background-alt = #111111
foreground = #B0B0B0
foreground-alt = #a2a073
foreground-ok = #63aa73
foreground-fail = #aa7373
primary = #ffb52a
secondary = #e60053
alert = #FF0000

[bar/example]
monitor = ${env:MONITOR:}
width = 100%
height = 22
radius = 0.0
fixed-center = true
top = yes
bottom = no
override-redirect = false
dpi = ${env:POLYBAR_DPI}

foreground = ${colors.foreground-alt}
background = ${colors.background-alt}

; Under-/overline pixel size and argb color
; Individual values can be defined using:
;   {overline,underline}-size
;   {overline,underline}-color
line-size = 1
line-color = #dfdfdf

border-top-size = 0
border-bottom-size = 0
border-left-size = 0
border-right-size = 0

padding-left = 0
padding-right = 0

module-margin-left = 0
module-margin-right = 0

;font-0 = Hack:pixelsize=9;3
font-0 = FontAwesome:style=Regular:pixelsize=10;3
font-2 = "Wuncon Siji:pixelsize=10;3"
font-1 = FontAwesome5Free:style=Solid:pixelsize=10;3
; font-1 = Weather Icons:style=Regular:size=10;2
font-3 = FontAwesome5Free:style=Solid:pixelsize=10;3
font-4 = FontAwesome5Brands:style=Solid:pixelsize=10;3


modules-left = i3 sp2
modules-right = sp1 workspace0 sp0 linux-ver sp1 volume sp1 battery sp1 wifi-network sp1 wired-network sp2 date sp2 shutdownrestart sp3

tray-position = right
tray-padding = 0
tray-detached = false
tray-maxsize = 14
tray-scale = 1.0
tray-background = ${colors.background-alt}
tray-foreground = ${colors.foreground-alt}
tray-offset-x = 0
tray-offset-y = 0

wm-restack = i3

scroll-up = #i3.next
scroll-down = #i3.prev

cursor-click = pointer

[module/sp0]
type = custom/text
content = " |   | "
content-foreground = ${colors.foreground-alt}
content-background = ${colors.background-alt}

[module/sp1]
type = custom/text
content = " | "
content-foreground = ${colors.foreground-alt}
content-background = ${colors.background-alt}

[module/sp2]
type = custom/text
content = " |"
content-foreground = ${colors.foreground-alt}
content-background = ${colors.background-alt}

[module/sp3]
type = custom/text
content = "|"
content-foreground = ${colors.foreground-alt}
content-background = ${colors.background-alt}

[module/i3]
type = internal/i3
format = <label-state> <label-mode>
index-sort = true
wrapping-scroll = false
strip-wsnumbers = false
enable-click = false
enable-scroll = false

label-mode = %mode%
label-mode-padding = 1
label-mode-foreground = ${colors.foreground-alt}
label-mode-background = ${colors.background-alt}

; focused = Active workspace on focused monitor
label-focused = %index%
label-focused-foreground = ${colors.foreground}
label-focused-background = ${colors.background}
label-focused-underline = ${colors.foreground-alt}
label-focused-padding = 1

; unfocused = Inactive workspace on any monitor
label-unfocused = %index%
label-unfocused-padding = ${self.label-focused-padding}
label-unfocused-foreground = ${colors.foreground-alt}
label-unfocused-background = ${colors.background-alt}

; visible = Active workspace on unfocused monitor
label-visible = %index%
label-visible-foreground = ${self.label-focused-background}
label-visible-background = ${self.label-focused-foreground}
label-visible-underline = ${self.label-focused-underline}
label-visible-padding = ${self.label-focused-padding}

; urgent = Workspace with urgency hint set
label-urgent = %index%
label-urgent-foreground = ${colors.foreground}
label-urgent-background = ${colors.alert}
label-urgent-padding = 1

; Separator in between workspaces
label-separator =
label-separator-padding = 0
label-separator-foreground = #fff

[module/shutdownrestart]
type = custom/text
label = %{A1:~/.config/scripts/shutdownrestart:}%{A}

[module/workspace0]
type = custom/text
label = %{A1:~/.config/scripts/enterworkspace:}sorc %{A}

[module/linux-ver]
type = custom/script
exec = uname -r
interval = 600

[module/date]
type = internal/date
interval = 1

time = "%a %d %I:%M %P"

format-prefix = " "
format-prefix-foreground = ${colors.foreground-alt}
format-prefix-background = ${colors.background-alt}

label = %time%
label-foreground = ${colors.foreground-alt}
label-background = ${colors.background-alt}

[module/volume]
type = internal/alsa

; Soundcard to be used
; Usually in the format hw:# where # is the card number
; You can find the different card numbers in `/proc/asound/cards`
master-soundcard = default
speaker-soundcard = default
headphone-soundcard = default

; Name of the master, speaker and headphone mixers
; Use the following command to list available mixer controls:
; $ amixer scontrols | sed -nr "s/.*'([[:alnum:]]+)'.*/\1/p"
; If master, speaker or headphone-soundcard isn't the default,
; use `amixer -c # scontrols` instead where # is the number
; of the master, speaker or headphone soundcard respectively
;
; Default: Master
master-mixer = Master
ramp-volume-0 = 
ramp-volume-1 = 
ramp-volume-2 = 

label-volume = "%percentage%%"
label-volume-foreground = ${colors.foreground-alt}
label-volume-background = ${colors.background-alt}
format-volume = <ramp-volume> <label-volume>

;format-muted-foreground = ${colors.foreground-alt}
format-muted-foreground = ${colors.foreground-fail}
format-muted-background = ${colors.background-alt}
label-muted = MUTED  


[module/battery]
type = internal/battery

full-at = 99
low-at = 5

battery = BAT0
adapter = ADP1

poll-interval = 5

ramp-capacity-0 = 
ramp-capacity-1 = 
ramp-capacity-2 = 
ramp-capacity-3 = 
ramp-capacity-4 = 

label-discharging = "%percentage%%"
format-discharging = <label-discharging> <ramp-capacity>

label-charging = "%percentage%%"
format-charging = <label-charging> <ramp-capacity>


label-full = "full  "
label-full-foreground = ${colors.foreground-ok}
label-low = "%percentage%%  "
label-low-foreground = #FFaaaa

[module/wired-network]

type = internal/network
interface = enp4s0f3u2u4
interface-type = wired
interval = 3.0
accumulate-stats = true
unknown-as-up = true
label-connected = %linkspeed% 
label-connected-foreground = ${colors.foreground-ok}

label-disconnected = 
label-disconnected-foreground = ${colors.foreground-fail}

[module/wifi-network]
type = internal/network
interface = wlan0

;label-connected = %{A1:nm-connection-editor:}%essid%%{A}

; format-connected = <ramp-signal> <label-connected>
label-connected = %{A1:~/.config/scripts/wifimanager:}%essid% %{A}
label-connected-foreground = ${colors.foreground-ok}

label-disconnected = "disconnected "
label-disconnected-foreground = ${colors.foreground-fail}

interface-type = wireless

interval = 3.0
accumulate-stats = true

unknown-as-up = true

speed-unit = ''
click-left= nm-connection editor


[settings]
throttle-output = 5
throttle-output-for = 10
;throttle-input-for = 30
screenchange-reload = true
compositing-background = over
compositing-foreground = over
compositing-overline = over
compositing-border = over

[global/wm]
margin-top = 5
margin-bottom = 5
